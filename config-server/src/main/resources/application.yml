server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/eddy16/eshop-config-repo
  application:
    name: config-server
security:
  user:
    name: root
    password: s3cr3t7

eureka:
  client:
    region: default
    registry-fetch-interval-seconds: 5
    service-url:
      defaultZone: http://localhost:8082/eureka/
